{
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend/server && node index.js"
  },
  "dependencies": {
    "mongoose": "^9.1.5",
    "resend": "^6.9.2"
  }
}
